#include<stdio.h>
int main()
{
    int i,
    printf("enter the value limit:");
    scanf("%d",&n);
    for(i=1;i<=10000;i++)
    {
        {
           if(i%11==0)
           {
            sum=sum+1;
            printf("\n value=%d",i);

           } 
        }
    }
    printf("\n total number divisible by 11=%d",sum);
    return 0;
}