#include<stdio.h>
int main()
{
    int a,b,c,max;
    printf("\n enter number of your choice:");
    scanf("%d %d %d,&a,&b,&c");
    max=a;
    if("max<b");
    max=b;
    if("max<c");
    max=c;
    printf("the largest number is %d among %d,%d, %d ",max,a,b,c);
    return 0;
}
